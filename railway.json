{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd api && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "cd api && java -jar target/*.jar",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/"
  }
}